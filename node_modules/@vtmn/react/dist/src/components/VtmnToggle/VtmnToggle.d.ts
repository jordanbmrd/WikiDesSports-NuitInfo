import * as React from 'react';
import '@vtmn/css-toggle/dist/index-with-vars.css';
import { VtmnToggleSize } from './types';
export interface VtmnToggleProps extends Omit<React.ComponentPropsWithoutRef<'input'>, 'size'> {
    /**
     * ID of the toggle.
     * @type {string}
     */
    identifier: string;
    /**
     * Label text linked to the toggle.
     * @type {string}
     */
    labelText: string;
    /**
     * The size of the toggle.
     * @defaultValue 'medium'
     */
    size?: VtmnToggleSize;
    /**
     * Label text linked to the toggle.
     * @type {boolean}
     * @defaultValue false
     */
    checked?: boolean;
    /**
     * Disabled state of the toggle.
     * @type {boolean}
     */
    disabled?: boolean;
}
export declare const VtmnToggle: ({ className, identifier, labelText, size, checked, disabled, ...props }: VtmnToggleProps) => JSX.Element;
declare const MemoVtmnToggle: React.MemoExoticComponent<({ className, identifier, labelText, size, checked, disabled, ...props }: VtmnToggleProps) => JSX.Element>;
export default MemoVtmnToggle;
